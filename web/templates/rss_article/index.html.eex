Hello world!

<%= Enum.map @rss_articles, fn(article) -> %>
  <h2>
    <a href="<%= article.url %>">
      <%= article.title %>
    </a>
  </h2>
  

  <p>
    <%= raw article.description %>
  </p>
  
  <hr>
<% end %>